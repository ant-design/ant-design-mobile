interface TagProps {
  /** whether button is disabled*/
  disabled?: boolean;
  size?: 'large'|'small';
  closable?: boolean;
  selected?: boolean;
  /** web only */
  prefixCls?: string;
}

export default TagProps;
