import * as React from 'react';
import { SearchBar } from 'antd-mobile';

export default React.createClass({
  render() {
    return (
      <SearchBar value="xxxxx" placeholder="xxxx" />
    );
  },
});
