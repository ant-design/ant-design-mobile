export interface TimelinePropsType {
  /** web only */
  prefixCls?: string;
  /** web only */
  color?: 'blue' | 'orange' | 'green';
  className?: string;
  pending?: boolean;
  children?: any;
}

export default TimelinePropsType;
