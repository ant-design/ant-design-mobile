export interface TimelinePropsType {
  color?: 'blue' | 'orang' | 'green';
  /** web only */
  prefixCls?: string;
  /** web only */
  className?: string;
  /** web only */
  pending?: boolean;
}

export default TimelinePropsType;
