export interface WhiteSpaceProps {
  size?: number;
  onClick?: () => void;
  /** web only */
  prefixCls?: string;
  /** web only */
  style?: React.CSSProperties;
  /** web only */
  className?: string;
}

export default WhiteSpaceProps;
