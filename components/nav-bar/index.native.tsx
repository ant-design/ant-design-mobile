import React from 'react';

export default class NavBar extends React.Component<object, any> {
  componentDidMount() {
    // tslint:disable-next-line:no-console
    console.warn(
      'NavBar does not support react-native, please use react-native navigator instead. ',
    );
  }
  render() {
    return null;
  }
}
